<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MarcaDeChoferes" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/status_barColor</item>
        <item name="colorPrimaryDark">@color/status_barColor</item>
        <item name="colorAccent">@color/status_barColor</item>
        <item name="fontFamily">@font/open_sans_regular</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="textView">
        <item name="fontFamily">@font/open_sans_regular</item>
    </style>
</resources>